Function EnumerationInterval(a, b) {

    let SetInterval = setInterval(() => {

        if(a <= b) {
            console.log(a);
            x++;

        } else {
            clearInterval(SetInterval);
        }
    }, 1000);
}

EnumerationInterval(4, 15);

// Код срабатывает с ошибкой. На 7 строке вы пытаетесь увеличить на 1 переменную х, которая нигде не объявлена. Ключевое слово function также написано с ошибкой. Оно должно начинаться с маленькой буквы.
// Также обратите пожалуйста внимание на стиль кода. Названия, начинающиеся с большой буквы, допустимы только в некоторых случаях - имена классов и имена функций-конструкторов. В остальных случаях лучше использовать camelCase: т.е. вместо EnumerationInterval -> enumerationInterval и т.д.